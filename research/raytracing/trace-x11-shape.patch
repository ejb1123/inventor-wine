--- a/dlls/winex11.drv/bitblt.c
+++ b/dlls/winex11.drv/bitblt.c
@@ -1829,6 +1829,9 @@
                 ptr[x] |= alpha_bits;
     }
 
+    if (window_surface->rect.right > 1000 && shape_changed)
+        WARN( "SURFACE_SHAPE hwnd %p window %lx surface %p rect %s dirty %s shape %p\n", window_surface->hwnd, surface->window, window_surface, wine_dbgstr_rect(rect), wine_dbgstr_rect(dirty), shape_bits );
+
     if (shape_changed)
     {
 #ifdef HAVE_LIBXSHAPE
